
# Enables the daily scheduled OSINT refresh (Vercel Cron sends this as a Bearer token)
CRON_SECRET=

# Comma-separated origins allowed to call the API cross-origin (default: same-origin only)
ALLOWED_ORIGINS=
